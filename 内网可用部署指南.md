# 内网可用部署指南

本文档记录本项目在没有公网、没有 Cloudflare、甚至荒郊野岭只有本地 WiFi 时的推荐部署方式。目标是：网页能打开、能登录、能看传感器、能控制 LED/FAN。

## 一、推荐架构

离线内网模式建议使用 HTTP，不强行折腾本地 HTTPS。

```text
手机 / 电脑浏览器
  -> http://树莓派A_IP:30082
  -> K3S nginx service
  -> K3S backend service
  -> MQTT / LoRa gateway
  -> Pi B / Pi C
```

当前 K3S 入口：

```text
前端 + /api 入口：http://192.168.10.70:30082
后端直连测试：http://192.168.10.70:30081
```

生产公网仍然可以继续走：

```text
https://iothutao.fyi
```

内网离线则走：

```text
http://192.168.10.70:30082
```

如果以后树莓派 A 做热点，建议固定成类似：

```text
http://192.168.50.1:30082
```

## 二、为什么内网用 HTTP

浏览器登录依赖 Cookie：

- 公网 HTTPS：Cookie 可以设置 `Secure`
- 内网 HTTP：Cookie 不能设置 `Secure`

如果内网 HTTP 还用 `Secure` Cookie，现象就是：

```text
登录接口返回 200
但浏览器不保存登录 Cookie
点控制硬件又跳回登录
```

所以离线 HTTP 模式必须让后端使用：

```text
COOKIE_SECURE=false
ALLOWED_ORIGINS=http://192.168.10.70:30082
```

公网 HTTPS 模式建议使用：

```text
COOKIE_SECURE=true
ALLOWED_ORIGINS=https://iothutao.fyi
```

注意：不要同时用公网域名和内网 IP 混着登录测试。Cookie、Origin、CSRF 都和访问来源有关。

## 三、迁移到新树莓派 A 的步骤

### 1. 基础环境

安装并启用这些服务：

```bash
sudo apt update
sudo apt install -y git docker.io
sudo systemctl enable --now docker
```

安装 K3S：

```bash
curl -sfL https://get.k3s.io | sh -
```

检查：

```bash
sudo k3s kubectl get nodes
sudo k3s kubectl get pods -A
```

### 2. 拉取项目

```bash
cd /home/pi
git clone https://github.com/Aprilnim/rasberrypi_iot_web.git temp_web
cd /home/pi/temp_web
```

如果是已有目录：

```bash
cd /home/pi/temp_web
git pull
```

### 3. 准备数据库

后端登录依赖 SQLite：

```text
/home/pi/yl40iot.db
```

迁移时要把旧设备上的数据库复制过来：

```bash
scp pi@旧树莓派A_IP:/home/pi/yl40iot.db /home/pi/yl40iot.db
```

检查：

```bash
ls -l /home/pi/yl40iot.db
```

如果登录接口 500，第一优先检查数据库有没有挂进去。

### 4. 准备密钥

项目目录下 `.env` 至少需要：

```text
CSRF_SIGNING_SECRET=随机长字符串
LORA_HMAC_SECRET=和 Pi B 一致的 LoRa 密钥
MQTT_HOST=192.168.10.70
MQTT_PORT=1883
MQTT_BACKEND_USERNAME=backend
MQTT_BACKEND_PASSWORD=123
MQTT_GATEWAY_USERNAME=lora-gateway
MQTT_GATEWAY_PASSWORD=123
ALLOWED_ORIGINS=http://192.168.10.70:30082
COOKIE_SECURE=false
```

生成新密钥：

```bash
openssl rand -hex 32
```

LoRa 密钥必须同时配置在：

```text
Pi A: lora-gateway / backend 环境变量
Pi B: ~/.receiver_b.env 或 ~/.pi_b_node.env
```

## 四、K3S 部署检查

当前推荐 K3S 服务名：

```bash
sudo k3s kubectl get deployment
sudo k3s kubectl get svc -o wide
```

应该看到类似：

```text
yl40-backend-service   NodePort   8080:30081
yl40-nginx-service     NodePort   80:30082
yl40-lora-gateway      Running
```

本机测试：

```bash
curl -i http://127.0.0.1:30082/
curl -i http://127.0.0.1:30082/api/lora/status
curl -i http://127.0.0.1:30082/api/led
curl -i http://127.0.0.1:30082/api/fan
```

局域网电脑测试：

```powershell
curl.exe -i "http://192.168.10.70:30082/api/lora/status"
```

## 五、离线 HTTP 登录测试

在树莓派 A 上测试：

```bash
COOKIE=/tmp/offline-cookie.txt
rm -f "$COOKIE"

curl -i -c "$COOKIE" --max-time 10 -X POST http://192.168.10.70:30082/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"你的密码"}'
```

正确结果里 `set-cookie` 不应该出现 `Secure`：

```text
set-cookie: control_token=...; HttpOnly; Max-Age=86400; Path=/api; SameSite=lax
set-cookie: csrf_token=...; Max-Age=86400; Path=/; SameSite=lax
```

如果这里出现 `Secure`，说明 K3S backend 没用到新镜像，或者 `COOKIE_SECURE=false` 没生效。

检查：

```bash
sudo k3s kubectl exec deployment/yl40-backend -- printenv COOKIE_SECURE
sudo k3s kubectl exec deployment/yl40-backend -- printenv ALLOWED_ORIGINS
sudo k3s kubectl exec deployment/yl40-backend -- sh -c "grep -n 'COOKIE_SECURE' /app/main.py"
```

期望：

```text
false
http://192.168.10.70:30082
```

## 六、K3S 更新代码的方法

K3S 不会自动使用普通 Docker 的新镜像。更新后端或网关时，需要 build、import、set image。

### 更新后端

```bash
cd /home/pi/temp_web

docker build -t temp_web-backend:offline-cookie ./backend
docker save temp_web-backend:offline-cookie | sudo k3s ctr images import -

sudo k3s kubectl set image deployment/yl40-backend \
  backend=docker.io/library/temp_web-backend:offline-cookie

sudo k3s kubectl rollout status deployment/yl40-backend
sudo k3s kubectl logs --tail=100 deployment/yl40-backend
```

### 更新 LoRa 网关

```bash
cd /home/pi/temp_web

docker build -t temp_web-lora-gateway:latest ./lora_gateway
docker save temp_web-lora-gateway:latest | sudo k3s ctr images import -

sudo k3s kubectl rollout restart deployment/yl40-lora-gateway
sudo k3s kubectl rollout status deployment/yl40-lora-gateway
sudo k3s kubectl logs --tail=100 deployment/yl40-lora-gateway
```

如果用了新 tag：

```bash
sudo k3s kubectl set image deployment/yl40-lora-gateway \
  lora-gateway=docker.io/library/temp_web-lora-gateway:新tag
```

### 更新前端

前端目录是 hostPath 挂载，通常最简单：

```bash
cd /home/pi/temp_web
git pull
```

然后浏览器强刷即可。必要时重启 nginx：

```bash
sudo k3s kubectl rollout restart deployment/yl40-nginx
sudo k3s kubectl rollout status deployment/yl40-nginx
```

## 七、Pi B / Pi C 迁移要点

### Pi B

Pi B 不连 MQTT，只跑 LoRa 节点脚本。

密钥文件示例：

```bash
cat > ~/.receiver_b.env <<'EOF'
export LORA_HMAC_SECRET=你的LoRa密钥
EOF

chmod 600 ~/.receiver_b.env
```

启动脚本示例：

```bash
cat > ~/start_pi_b_node.sh <<'EOF'
#!/bin/bash
set -e

source ~/.receiver_b.env
python3 /home/pi/pi_b_node.py
EOF

chmod +x ~/start_pi_b_node.sh
```

启动：

```bash
~/start_pi_b_node.sh
```

### Pi C

Pi C 连 MQTT，发布 SHT35 和 YL40。

检查脚本是否运行：

```bash
pgrep -af pi_c_rs485_node.py
```

启动：

```bash
python3 ~/pi_c_rs485_node.py
```

## 八、EMQX

目前 EMQX 仍可继续跑 Docker，没必要急着迁到 K3S。

检查：

```bash
docker ps | grep emqx
```

只要 backend、lora-gateway、Pi C 都能连接 MQTT，就先保持稳定。

以后真要迁移 EMQX，先备份：

```text
用户
密码
ACL
Dashboard 配置
```

## 九、常见坑

### 1. 登录 200 但控制硬件又弹登录

原因通常是 Cookie 没保存。

内网 HTTP 必须：

```text
COOKIE_SECURE=false
ALLOWED_ORIGINS=http://192.168.10.70:30082
```

改完后清浏览器站点数据，重新登录。

### 2. 登录接口 500

优先检查数据库：

```bash
ls -l /home/pi/yl40iot.db
sudo k3s kubectl exec deployment/yl40-backend -- ls -l /home/pi/yl40iot.db
```

K3S manifest 里要把宿主机数据库挂进去。

### 3. 直接访问 `http://192.168.10.70:30082` 正常，公网异常

检查 Cloudflare Tunnel origin 是否指向：

```text
http://127.0.0.1:30082
```

重启：

```bash
sudo systemctl restart cloudflared
sudo systemctl status cloudflared --no-pager
```

### 4. 本地 HTTPS / iot.local 很折腾

今晚踩坑结论：K3S 的 Traefik / ServiceLB 可能占用或劫持 443，本地证书又要每台客户端信任，手机更麻烦。

所以无公网离线模式优先用：

```text
http://树莓派A_IP:30082
```

这条路最稳、最少配置、最适合野外使用。

### 5. 网关 GPIO 报错

检查是否还有旧 Docker 网关占用串口/GPIO：

```bash
docker ps | grep lora
ps -ef | grep lora_mqtt | grep -v grep
```

K3S 网关需要设备权限：

```text
/dev/ttyS0
/dev/gpiomem
/dev/gpiochip0
```

## 十、迁移验收清单

新设备部署完，按顺序测：

```bash
curl -i http://127.0.0.1:30082/
curl -i http://127.0.0.1:30082/api/sensor
curl -i http://127.0.0.1:30082/api/lora/status
curl -i http://127.0.0.1:30082/api/led
curl -i http://127.0.0.1:30082/api/fan
```

浏览器测试：

```text
1. 打开 http://树莓派A_IP:30082
2. 登录
3. 查看温湿度、光照
4. 查看设备 B / 设备 C 在线状态
5. 开关 LED/FAN
6. 打开第二个浏览器窗口，确认数字孪生状态能同步
```

如果这些都通过，说明内网离线部署可用。
