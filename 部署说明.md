# 部署说明

本文档说明如何在树莓派 A 上启动本项目的 Docker 服务、Nginx 前端反向代理，以及 Cloudflare Tunnel 公网访问。

当前项目部署结构：

- FastAPI 后端运行在 Docker 容器 `yl40-backend` 中，容器内端口为 `8080`。
- Nginx 运行在 Docker 容器 `yl40-nginx` 中，对外暴露宿主机 `80` 端口。
- Nginx 负责提供 `frontend/` 静态网页，并把 `/api/` 请求代理到后端。
- Cloudflare Tunnel 负责把公网 HTTPS 域名转发到树莓派 A 的本地 `http://localhost:80`。

## 一、进入项目目录

先 SSH 登录树莓派 A：

```bash
ssh pi@192.168.10.70
```

进入项目目录：

```bash
cd /home/pi/temp_web
```

确认项目文件存在：

```bash
ls
```

正常应能看到：

```text
backend  frontend  nginx  docker-compose.yml
```

## 二、启动 Docker 服务

如果 Docker 没有启动，先启动 Docker：

```bash
sudo systemctl start docker
```

设置 Docker 开机自启：

```bash
sudo systemctl enable docker
```

查看 Docker 状态：

```bash
sudo systemctl status docker
```

如果显示 `active (running)`，说明 Docker 正常运行。

## 三、启动项目容器

在项目目录 `/home/pi/temp_web` 下执行：

```bash
docker compose up -d --build
```

这条命令会启动两个容器：

```text
yl40-backend
yl40-nginx
```

查看容器状态：

```bash
docker compose ps
```

正常情况下应看到两个服务都是 `running` 或 `Up`。

查看后端日志：

```bash
docker logs -f yl40-backend
```

查看 Nginx 日志：

```bash
docker logs -f yl40-nginx
```

停止查看日志时按：

```text
Ctrl + C
```

## 四、启动 Nginx 服务

本项目的 Nginx 不是宿主机上的独立 nginx，而是 Docker Compose 中的 `nginx` 服务。

只启动 Nginx 容器：

```bash
docker compose up -d nginx
```

重启 Nginx 容器：

```bash
docker compose restart nginx
```

查看 Nginx 容器是否启动：

```bash
docker ps | grep yl40-nginx
```

本地测试 Nginx 首页：

```bash
curl -I http://localhost/
```

局域网电脑访问：

```text
http://192.168.10.70/
```

如果网页能打开，说明 Nginx 静态页面服务正常。

## 五、测试后端 API

在树莓派 A 上测试：

```bash
curl -i http://localhost/api/sensor
curl -i http://localhost/api/led
curl -i http://localhost/api/fan
curl -i http://localhost/api/lora/status
```

在 Windows PowerShell 中测试：

```powershell
curl.exe -i "http://192.168.10.70/api/sensor"
curl.exe -i "http://192.168.10.70/api/led"
curl.exe -i "http://192.168.10.70/api/fan"
curl.exe -i "http://192.168.10.70/api/lora/status"
```

如果这些接口返回 `200 OK` 和 JSON 数据，说明 Nginx 到后端的 `/api/` 代理正常。

## 六、启动 Cloudflare Tunnel

Cloudflare Tunnel 的作用是把公网 HTTPS 域名转发到树莓派 A 的本地 Nginx。

当前项目中 Nginx 监听宿主机 `80` 端口，所以 Tunnel 目标地址应为：

```text
http://localhost:80
```

### 方式 A：临时 trycloudflare 隧道

如果你使用的是临时公网地址，可以执行：

```bash
cloudflared tunnel --url http://localhost:80
```

启动后终端会输出一个公网地址，类似：

```text
https://xxxx-xxxx-xxxx.trycloudflare.com
```

浏览器访问这个地址即可打开网页。

注意：这个临时地址可能会变化，重启 Tunnel 后通常会生成新的地址。

### 方式 B：systemd 服务方式

如果你已经把 Cloudflare Tunnel 安装成系统服务，可以启动：

```bash
sudo systemctl start cloudflared
```

设置开机自启：

```bash
sudo systemctl enable cloudflared
```

查看运行状态：

```bash
sudo systemctl status cloudflared
```

查看日志：

```bash
journalctl -u cloudflared -f
```

如果看到连接成功、没有持续报错，说明 Tunnel 正常。

## 七、生产访问检查

假设 Cloudflare 公网域名是：

```text
https://你的-cloudflare-域名/
```

检查网页：

```powershell
curl.exe -i "https://你的-cloudflare-域名/"
```

检查接口：

```powershell
curl.exe -i "https://你的-cloudflare-域名/api/sensor"
curl.exe -i "https://你的-cloudflare-域名/api/lora/status"
```

如果网页和 API 都返回 `200 OK`，说明公网访问链路正常。

完整链路是：

```text
浏览器
  -> Cloudflare HTTPS 域名
  -> Cloudflare Tunnel
  -> 树莓派 A localhost:80
  -> Docker Nginx
  -> frontend 静态页面 / backend API
```

## 八、常用维护命令

重新构建并启动全部服务：

```bash
cd /home/pi/temp_web
docker compose up -d --build
```

停止全部服务：

```bash
docker compose down
```

重启后端：

```bash
docker compose restart backend
```

重启 Nginx：

```bash
docker compose restart nginx
```

查看容器状态：

```bash
docker compose ps
```

查看后端最近日志：

```bash
docker logs --tail=100 yl40-backend
```

查看 Nginx 最近日志：

```bash
docker logs --tail=100 yl40-nginx
```

查看 80 端口是否被监听：

```bash
sudo ss -lntp | grep ':80'
```

## 九、注意事项

- 正式前端文件在 `frontend/` 目录，不要再修改已经删除的 `nginx/html/` 旧测试文件。
- `docker-compose.yml` 当前挂载的是 `./frontend:/usr/share/nginx/html`。
- 修改前端后，通常只需要重新上传 `frontend/index.html`、`frontend/style.css`、`frontend/app.js`，Nginx 容器不一定需要重建。
- 修改后端 Python 代码后，需要执行 `docker compose up -d --build` 重新构建后端镜像。
- Cloudflare Tunnel 必须指向 `http://localhost:80`，不要直接指向后端 `8080`，否则前端静态页面不会由 Nginx 正常提供。
- 树莓派 B 的 `receiver_b.py` 不在 Docker 中运行，需要单独启动和单独部署。

