# 项目踩坑记录

## 项目简介

树莓派 IoT 传感器监控 + LoRa 远程 LED 控制项目。

- **树莓派 A**：运行 Docker 容器，包含 FastAPI 后端 + Nginx 前端，通过 LoRa 发送 LED 开关命令
- **树莓派 B**：运行独立的 `receiver_b.py`，接收 LoRa 命令并控制 GPIO18 LED

---

## 坑 1：Dockerfile 只复制了 main.py，漏了 lora.py

### 现象

```
ModuleNotFoundError: No module named 'lora'
```

### 原因

Dockerfile 使用：

```dockerfile
COPY main.py .
```

只复制了 `main.py` 到镜像，`lora.py` 根本没进去。容器启动后 `from lora import LoRaNode` 找不到模块。

### 修复

```dockerfile
COPY . .
```

把整个 `backend/` 目录复制到镜像内。

---

## 坑 2：requirements.txt 漏了 RPi.GPIO

### 现象

Docker build 或容器启动时报：

```
ModuleNotFoundError: No module named 'RPi'
```

### 原因

`lora.py` 内部 `import RPi.GPIO as GPIO`，但 `requirements.txt` 只写了 `pyserial`，没写 `RPi.GPIO`。

### 修复

```text
fastapi
uvicorn
smbus2
gpiozero
pyserial
RPi.GPIO
```

---

## 坑 3：python:3.11-slim 镜像缺少 gcc，RPi.GPIO 编译失败

### 现象

`pip install RPi.GPIO` 时编译报错：

```
error: command 'gcc' failed: No such file or directory
```

### 原因

`RPi.GPIO` 是 C 语言扩展模块，安装时需要本地编译。`python:3.11-slim` 是精简镜像，默认不带 `gcc` 和 `python3-dev`。

### 修复

在 `Dockerfile` 的 `pip install` 之前增加：

```dockerfile
RUN apt-get update && apt-get install -y gcc python3-dev && rm -rf /var/lib/apt/lists/*
```

> **注意**：装完 `gcc` 后要把 apt 缓存删掉，减小镜像体积。

---

## 坑 4：docker-compose.yml 漏了串口设备映射

### 现象

容器内打开 `/dev/ttyS0` 失败，LoRa 初始化报错：

```
[Errno 2] No such file or directory: '/dev/ttyS0'
```

### 原因

虽然宿主机有 `/dev/ttyS0`，但 Docker 容器默认看不到。需要显式映射 `devices`。

### 修复

```yaml
devices:
  - "/dev/i2c-1:/dev/i2c-1"
  - "/dev/ttyS0:/dev/ttyS0"
```

---

## 坑 5：LORA_AVAILABLE 全局变量多余，导致判断不一致

### 现象

理论上不是严重 bug，但存在维护隐患。

### 原因

同时维护两个状态标志：
- `LORA_AVAILABLE` 全局变量
- `lora.available` 实例属性

如果初始化流程有变化，可能出现两者不一致。

### 修复

删掉 `LORA_AVAILABLE` 全局变量，所有判断统一用：

```python
if lora is None or not lora.available:
```

---

## 坑 6：前端 LED 显示和实际状态不同步

### 现象

前端页面显示 "通信协议：I2C + GPIO"，但实际 LED 控制已改为 LoRa。

### 修复

```html
<span class="info-val">I2C + LoRa</span>
```

---

## 正确部署命令

在 **树莓派 A** 上：

```bash
# 1. 确保树莓派 B 已启动 receiver_b.py
cd /path/to/project/backend
sudo python3 receiver_b.py

# 2. 树莓派 A 重新构建并启动 Docker
docker-compose down
docker-compose up --build -d

# 3. 查看后端日志确认 LoRa 初始化成功
docker logs -f yl40-backend
```

---

## 当前文件清单

```
backend/
├── Dockerfile           # 新增 gcc/python3-dev，COPY . .
├── main.py              # LED 控制改为 LoRa 发送
├── lora.py              # 新增：LoRaNode 封装类
├── receiver_b.py        # 新增：树莓派 B 接收脚本
└── requirements.txt     # 新增 pyserial、RPi.GPIO

frontend/
├── index.html           # 美化 + LED 开关 + 系统信息 + 日志
├── app.js               # 保留 API 调用，新增日志记录
└── style.css            # 深色科技风

docker-compose.yml       # 新增 /dev/ttyS0 映射
```

---

## 教训

1. **Dockerfile 复制文件要完整**：`COPY main.py .` → `COPY . .`
2. **requirements.txt 要写全**：只要代码里 import 了，就要写进去
3. **C 扩展需要编译工具**：`RPi.GPIO`、`smbus2` 等都需要 `gcc`
4. **串口/I2C 要映射进容器**：Docker 不是 VM，不会自动共享设备文件
5. **状态判断要单一来源**：不要同时维护多个状态标志

---

## MQTT 迁移部署踩坑记录

### 坑 7：MQTT override 文件没同步到树莓派 A

#### 现象

```bash
docker compose -f docker-compose.yml -f docker-compose.mqtt.yml config
open /home/pi/temp_web/docker-compose.mqtt.yml: no such file or directory
```

#### 原因

树莓派 A 的 `/home/pi/temp_web` 还没有拉到最新代码，`docker-compose.mqtt.yml` 不存在。

#### 修复

在树莓派 A 执行：

```bash
cd /home/pi/temp_web
git pull
```

或手动同步：

```powershell
scp .\docker-compose.mqtt.yml pi@192.168.10.70:/home/pi/temp_web/docker-compose.mqtt.yml
```

---

### 坑 8：`.env` 写好了，但容器里读不到

#### 现象

```bash
docker compose exec backend sh -c 'echo CSRF_SIGNING_SECRET=$CSRF_SIGNING_SECRET; echo LORA_HMAC_SECRET=$LORA_HMAC_SECRET'
CSRF_SIGNING_SECRET=
LORA_HMAC_SECRET=
```

#### 原因

只是本地创建了 `.env`，但还没有用新的 compose 配置重新创建容器，旧容器环境变量不会自动变化。

#### 修复

```bash
cd /home/pi/temp_web
docker compose -f docker-compose.yml -f docker-compose.mqtt.yml up -d --build
```

检查：

```bash
docker compose -f docker-compose.yml -f docker-compose.mqtt.yml config | grep -E "CSRF_SIGNING_SECRET|LORA_HMAC_SECRET"
docker compose -f docker-compose.yml -f docker-compose.mqtt.yml exec backend sh -c 'echo ${#CSRF_SIGNING_SECRET}; echo ${#LORA_HMAC_SECRET}'
```

---

### 坑 9：树莓派 B 直接运行 `pi_b_node.py` 找不到 LoRa 密钥

#### 现象

```bash
python3 pi_b_node.py
[Pi B] FATAL: missing LORA_HMAC_SECRET
```

#### 原因

`LORA_HMAC_SECRET` 是进程环境变量。当前终端有不代表新终端一直有；直接运行脚本时没有加载密钥文件。

#### 修复

在树莓派 B 持久化密钥：

```bash
cat > ~/.receiver_b.env <<'EOF'
export LORA_HMAC_SECRET=你的LoRa密钥
EOF
chmod 600 ~/.receiver_b.env
```

写启动脚本：

```bash
cat > ~/start_pi_b_node.sh <<'EOF'
#!/bin/bash
set -e

source ~/.receiver_b.env
python3 ~/pi_b_node.py
EOF
chmod +x ~/start_pi_b_node.sh
```

启动：

```bash
~/start_pi_b_node.sh
```

---

### 坑 10：`lora-gateway` 容器里 `RPi.GPIO` 判断“不是树莓派”

#### 现象

```text
RuntimeError: This module can only be run on a Raspberry Pi!
```

或：

```text
FileNotFoundError: /proc/device-tree/system/linux,revision
```

#### 原因

Docker 容器里不一定能读到树莓派的设备树 revision 文件。`RPi.GPIO` / `rpi-lgpio` 会靠这个判断当前机器是不是 Raspberry Pi。

#### 修复

`lora_gateway/requirements.txt` 使用：

```text
rpi-lgpio
```

并在 `docker-compose.mqtt.yml` 的 `lora-gateway.environment` 中显式指定 Pi 4 revision：

```yaml
- RPI_LGPIO_REVISION=${RPI_LGPIO_REVISION:-c03115}
```

---

### 坑 11：`apt-get install lgpio` 找不到包

#### 现象

```text
E: Unable to locate package lgpio
```

#### 原因

`python:3.11-slim` 使用的 Debian 源里没有名为 `lgpio` 的 apt 包。

#### 修复

不要在 Dockerfile 里安装 `lgpio` 这个 apt 包，改为 pip 安装 `rpi-lgpio`：

```dockerfile
RUN apt-get update \
    && apt-get install -y gcc python3-dev \
    && rm -rf /var/lib/apt/lists/* \
    && pip install --no-cache-dir -r requirements.txt
```

---

### 坑 12：`rpi-lgpio` 能识别 Pi 了，但打不开 gpiochip

#### 现象

```text
lgpio.error: 'can not open gpiochip'
```

#### 原因

`rpi-lgpio` 通过 Linux gpiochip 访问 GPIO，容器只挂 `/dev/gpiomem` 不够，还需要 `/dev/gpiochip0`。

#### 修复

`docker-compose.mqtt.yml` 的 `lora-gateway.devices` 增加：

```yaml
- "/dev/gpiochip0:/dev/gpiochip0"
```

部署后检查：

```bash
ls -l /dev/gpiochip0 /dev/gpiomem /dev/ttyS0
docker logs --tail=100 yl40-lora-gateway
```

看到下面日志表示网关已经连接 MQTT：

```text
[MQTT] Gateway connected
```

---

### 坑 13：公网 502，不一定是后端代码问题

#### 现象

```text
502 Bad Gateway
Unable to reach the origin service.
```

#### 原因

Cloudflare Tunnel 连不到树莓派 A 的本地 origin。实际排查发现只启动了 `lora-gateway` 和 `emqx`，没有启动 `yl40-backend` 和 `yl40-nginx`。

#### 修复

检查容器：

```bash
docker ps -a
```

启动 backend 和 nginx：

```bash
cd /home/pi/temp_web
docker compose -f docker-compose.yml -f docker-compose.mqtt.yml up -d --build backend nginx
```

先测本机，再测公网：

```bash
curl -i http://127.0.0.1/api/lora/status
curl -i http://127.0.0.1/api/light
```

---

### 坑 14：`hello-world` 容器不是项目的一部分

#### 现象

```text
hello-world   Exited (0)   focused_hodgkin
```

#### 原因

这是以前运行 Docker 官方测试镜像留下的退出容器，不占端口，不影响项目。

#### 修复

可删除：

```bash
docker rm focused_hodgkin
```

---

### 坑 15：命令行登录成功，但硬件控制返回 gateway/pi-b offline

#### 现象

```json
{"on":false,"available":false,"error":"MQTT gateway or pi-b offline","cmd_id":null}
```

#### 原因

登录、Cookie、CSRF 都已经成功，但 FastAPI 判断 MQTT 网关或 Pi B 不在线，所以没有真正发布硬件控制命令。

优先排查：

```bash
docker logs --tail=150 yl40-lora-gateway
curl -s http://127.0.0.1/api/lora/status && echo
pgrep -af pi_b_node.py
```

---

### 坑 16：EMQX WebSocket 订阅主题写错

#### 现象

backend 用户订阅失败，看不到网关或 Pi B 消息。

#### 原因

主题必须严格带 `/`。错误写法类似：

```text
yl40iotv1/gateways/lora-a#
```

正确写法：

```text
yl40iot/v1/gateways/lora-a/#
yl40iot/v1/nodes/pi-b/#
```

注意：

- `yl40iot` 和 `v1` 中间有 `/`
- `lora-a` 和 `#` 中间也要有 `/`
- `#` 必须单独占一级

---

### 坑 17：EMQX ACL 顺序导致 backend 订阅被拒绝

#### 现象

超级管理员可以订阅：

```text
yl40iot/v1/gateways/lora-a/#
yl40iot/v1/nodes/pi-b/#
```

但 backend 用户订阅失败，即使看起来已经给了授权。

#### 原因

EMQX 授权规则按顺序匹配。若 `拒绝 #` 或 `deny all` 放在允许规则之前，会先命中拒绝，后面的 backend allow 没机会生效。

#### 修复

ACL 顺序应保持：

```text
1. 拒绝 retained 控制命令
2. backend allow
3. lora-gateway allow
4. pi-c allow
5. deny all / 拒绝 #
```

也就是说，backend、lora-gateway、pi-c 的允许规则必须放在最终兜底拒绝之前。

---

## MQTT 迁移阶段关键检查命令

### A 端查看容器

```bash
cd /home/pi/temp_web
docker ps -a
docker logs --tail=100 yl40-backend
docker logs --tail=100 yl40-lora-gateway
docker logs --tail=100 yl40-nginx
```

### A 端测试公网 API 前先测本机

```bash
curl -i http://127.0.0.1/api/lora/status
curl -i http://127.0.0.1/api/light
curl -i http://127.0.0.1/api/fan
curl -i http://127.0.0.1/api/led
```

### PowerShell 测公网读接口

```powershell
curl.exe -i "https://jazz-ambient-backing-printer.trycloudflare.com/api/lora/status"
curl.exe -i "https://jazz-ambient-backing-printer.trycloudflare.com/api/light"
curl.exe -i "https://jazz-ambient-backing-printer.trycloudflare.com/api/fan"
curl.exe -i "https://jazz-ambient-backing-printer.trycloudflare.com/api/led"
```

### PowerShell 登录并带 CSRF 控制硬件

```powershell
$base = "https://jazz-ambient-backing-printer.trycloudflare.com"
$cookie = "$env:TEMP\yl40-cookie.txt"

curl.exe -i -c $cookie -X POST "$base/api/auth/login" `
  -H "Content-Type: application/json" `
  -d '{\"username\":\"admin\",\"password\":\"2142\"}'

$csrf = (Select-String -Path $cookie -Pattern "csrf_token" | Select-Object -Last 1).Line.Split("`t")[-1]

curl.exe -i -b $cookie -X POST "$base/api/led" `
  -H "Content-Type: application/json" `
  -H "X-CSRF-Token: $csrf" `
  -d '{\"on\":true}'

curl.exe -i -b $cookie -X POST "$base/api/fan" `
  -H "Content-Type: application/json" `
  -H "X-CSRF-Token: $csrf" `
  -d '{\"on\":true}'
```
